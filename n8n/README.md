# n8n Workflows

This folder will contain **sanitized exports of n8n workflows** used in the AI content pipeline.

Secrets and credentials are removed before exporting.

Planned workflows:

- pipeline-main.json — main orchestration workflow
- render-job-handler.json — render job submission and polling
- retry-and-error-handler.json — failure handling patterns
